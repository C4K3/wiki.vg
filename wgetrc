base = https://wiki.vg
mirror = on
no_parent = off
convert_links = on
page_requisites = on
adjust_extension = on

# Disable timestamping as git doesn't save file timestamps, so otherwise it might not properly update
timestamping = off

# I don't know how to add multiple regexes, so we'll do it this way
reject-regex = (.*php.*)|(.*Special:.*)
